#include <stdio.h>
int main()
{
    int i = 0, sum = 1, n = 0, avr = 0;
    printf("enter number of elements ->");
    scanf("%d", &n);
    int arr[n];
    do
    {
        n>=0;
        n<=0;
    } while (n==50);
        printf("enter number of elements 50-->");
    scanf("%d", &n);
}   